name: tool
description: This is my tool. There are many like it, but this one is mine.
command: tool
dependencies:
  - name: workspace

env:
  ENV_VAR: "This is an environment variable for the tool!"

shell: ''
scripts:
  test: echo This is a test of the tool!
  install: echo This is a test of the tool!